**UI 设计红线（Anti-patterns）**:
- **禁紫色渐变白底** — AI 默认审美标志
- **禁 Emoji 代图标**（🔔📁⚙️）→ 用自绘 SVG 或声明的图标库
- **禁所有屏幕同一居中卡片布局** → 列表/详情/表单须有明显差异
- **禁纯黑+纯白** → 用有层次的灰度色板
- **禁通用感** → 打开须一眼判断审美方向（Linear vs Notion vs Grafana）
- **禁硬编码** — 颜色用 Token，间距用变量，非魔法数字
